services:
  mysql:
    image: mysql:8.0

    container_name: mysql-fraud

    restart: always

    environment:
      MYSQL_ROOT_PASSWORD: "gmail@t#k"
      MYSQL_DATABASE: fraudetectiondb

    ports:
      - "3307:3306"

    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data: